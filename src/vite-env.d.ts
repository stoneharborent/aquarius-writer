/// <reference types="vite/client" />

/**
 * The app's version, substituted at build time from package.json by
 * `define` in vite.config.ts. Never type the number into a component.
 */
declare const __APP_VERSION__: string;
