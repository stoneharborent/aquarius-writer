{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Aquarius Writer's main window. Deliberately narrow: the vault is reached through this app's own commands (which need no permission and do their own path checks), so the only plugin permission granted is the folder picker.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
